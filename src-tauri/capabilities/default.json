{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main Nexus AI window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
